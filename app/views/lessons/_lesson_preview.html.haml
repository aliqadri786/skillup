- if @lesson.present? && lesson == @lesson
  .shadow-sm.px-2.py-2.text-decoration-none.border.fw-bold
    .fa.fa-chevron-right
    = link_to course_lesson_path(@course,lesson) do
      -# = lesson.row_order_rank + 1
      = lesson.title
    
   
- else
  .shadow-sm.px-2.py-2.text-decoration-none.border
    = link_to  course_lesson_path(@course,lesson) do
      -# = lesson.row_order_rank + 1
      = lesson.title
 


